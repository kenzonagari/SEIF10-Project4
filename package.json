{  
  "scripts": {
    "dev": "turbo run dev",
    "install": "cd client && npm update && cd.. && cd server && npm update",
    "build": "turbo run build",
    "start": "cd server && npm run start"
  },
  "devDependencies": {
    "turbo": "^1.6.3"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
