{  
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "cd server && npm run start",
    "postinstall": "(cd server && npm install)"
  },
  "devDependencies": {
    "turbo": "^1.6.3"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
